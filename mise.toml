[tools]
# Node stays alongside Bun: scripts/check-evidence.ts runs under Node here and
# in CI (its shebang says so, and its test spawns `node` explicitly), and the
# Pagefind binary is a Node package.
node = "24"
bun = "1.3.14"
